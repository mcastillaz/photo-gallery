
    <%- include("../partials/head")  %>

<body>
        <%- include("../partials/navigation")  %>

    <div class="container p-5">
        <div class="row">
            <div class="col-md-7 offsed-md-7">         
                <div class="card animated fadeInDown">
                    <img src="<%= photo.path %>" class="card-img-top" alt="">
                    <div class="card-body">
                     <h5 class="aria-label">Detail</h5>
                      <h2 class="card-title"><%= photo.title %></h2>
                      <p><i class="far fa-calendar"></i> <%= format(photo.created_at) %></p>
                      <p><i class="far fa-image arial"></i> <%= photo.originalname %></br>   <%= photo.size %> KB</p>
                      <p> </p>
                      <a href="/photo/<%= photo._id %>/delete" class="btn btn-danger">
                        Delete
                     </a>
                     
                    </div>
                </div>
            </div> 
        </div>
    </div>

</body>
